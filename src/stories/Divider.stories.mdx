import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { DividerComponent } from "projects/rustic/src/lib/divider/divider.component";

<Meta
  title="Components/Divider"
  component={DividerComponent}
  argTypes={{
    type: {
      options: ["horizontal", "vertical"],
      control: { type: "radio" },
    },
  }}
/>

# List Item

---

### Horizontal Divider

export const Template = (args) => ({
  props: {
    propInput: {
      type: args.type,
    },
  },
  template: `<span>Lorem Ipsum</span><cis-divider [type]="propInput.type"></cis-divider><span>Secondly: Hello!</span>`,
});

<Canvas>
  <Story name="Horizontal" args={{ type: "horizontal" }}>
    {Template.bind({})}
  </Story>
</Canvas>

```html
<span>Lorem</span>
<cis-divider type="horizontal"></cis-divider>
<span>Ipsum</span>
```

---

### Vertical Divider

<Canvas>
  <Story name="Vertical" args={{ type: "vertical  " }}>
    {Template.bind({})}
  </Story>
</Canvas>

```html
<span>Lorem</span>
<cis-divider type="vertical"></cis-divider>
<span>Ipsum</span>
```

<ArgsTable of={DividerComponent} />
