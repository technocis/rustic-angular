import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { ToggleComponent } from "../../projects/rustic/src/lib/toggle/toggle.component";

<Meta
  title="Components/Toggle"
  component={ToggleComponent}
  argTypes={{
    label: {
      control: {
        type: "text",
      },
    },
  }}
/>

# Toggle

---

export const Template = (args) => ({
  component: ToggleComponent,
  props: {
    propInput: {
      label: args.label,
    },
  },
  template: `<cis-toggle [label]="propInput.label"><input type="checkbox"></cis-toggle>`,
});

<Canvas>
  <Story name="Toggle" args={{ label: "Do you want this?" }}>
    {Template.bind({})}
  </Story>
</Canvas>

```html
<cis-toggle label="Do you want this?">
  <input type="checkbox" />
</cis-toggle>
```

<ArgsTable story="Toggle" />
