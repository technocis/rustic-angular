import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { CommonModule } from "@angular/common";
import { moduleMetadata } from "@storybook/angular";
import { lightTheme, darkTheme } from "../themes.ts";
import { ThemeModule } from "../../projects/rustic/src/lib/theme/theme.module";
import { ListItemComponent } from "../../projects/rustic/src/lib/list-item/list-item.component";
import { TitleComponent } from "projects/rustic/src/lib/title/title.component";
import { SubtitleComponent } from "projects/rustic/src/lib/subtitle/subtitle.component";

<Meta
  title="Components/Lists/List Item"
  component={ListItemComponent}
  decorators={[
    moduleMetadata({
      declarations: [ListItemComponent, TitleComponent, SubtitleComponent],
      imports: [
        CommonModule,
        ThemeModule.forRoot({
          themes: [lightTheme, darkTheme],
          active: "light",
        }),
      ],
    }),
  ]}
/>

# List Item

export const Template = (args) => ({
  props: {
    propInput: {
      hover: true,
    },
  },
  template: `<cis-list-item> <cis-title>List Item 1</cis-title> </cis-list-item>`,
});

export const SubTemplate = (args) => ({
  props: {
    propInput: {
      hover: true,
    },
  },
  template: `<cis-list-item> <cis-title>List Item</cis-title> <cis-subtitle>With Subtitle Here</cis-subtitle> </cis-list-item>`,
});

<Canvas>
  <Story
    name="List Item"
    args={{
      hover: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<cis-list-item>
  <cis-title>List Item 1</cis-title>
</cis-list-item>
```

<Canvas>
  <Story
    name="With Subtitle"
    args={{
      hover: true,
    }}
  >
    {SubTemplate.bind({})}
  </Story>
</Canvas>

```html
<cis-list-item>
  <cis-title>List Item</cis-title>
  <cis-subtitle>With Subtitle Here</cis-subtitle>
</cis-list-item>
```
